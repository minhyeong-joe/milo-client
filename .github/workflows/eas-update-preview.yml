name: EAS Update Preview

on:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  update-preview:
    name: Publish preview update
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Publish EAS Update
        env:
          APP_VARIANT: preview
        run: |
          eas update --platform ios --channel preview --environment preview --non-interactive --message "Preview iOS update: ${{ github.sha }}"
          eas update --platform android --channel preview --environment preview --non-interactive --message "Preview Android update: ${{ github.sha }}"
